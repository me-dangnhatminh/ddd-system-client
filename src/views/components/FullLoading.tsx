function FullLoading() {
  return (
    <div
      style={{
        overflow: "hidden !important",
        width: "100vw",
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Loading...
    </div>
  );
}

export default FullLoading;
